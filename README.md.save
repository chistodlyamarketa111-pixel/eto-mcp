# ETO Travel MCP Server

MCP-compatible HTTP server for searching tours via eto.travel (Tourvisor backend).

Проект реализует MCP-сервер на FastAPI, который предоставляет инструмент
`search_tours` и возвращает реальные туры с ценами, датами, ночами, питанием
и информацией об отелях.

---

## 🚀 Возможности

- MCP-style API (HTTP)
- Реальная интеграция с backend eto.travel (Tourvisor)
- Получение живых туров:
  - цена
  - дата вылёта
  - количество ночей
  - тип питания
  - город
  - отель (название, звёзды, рейтинг, фото)
- Swagger UI (`/docs`)
- Демонстрационный агент-клиент (LLM-like), использующий MCP API

---

## 🧠 Архитектура

Поиск туров в Tourvisor состоит из двух этапов:

1. **Start search** — создаёт `requestid` (на стороне frontend eto.travel)
2. **Get results** — polling результатов поиска по `requestid`

В рамках тестового задания реализован второй этап:
сервер принимает `requestid` и получает результаты через endpoint:

https://search3.tourvisor.ru/modresult.php


> `requestid` берётся из браузерного поиска eto.travel (DevTools → Network).

---

## 📡 MCP API

### `POST /mcp/get_results`

Получить результаты поиска по `requestid`.

**Request**
```json
{
  "requestid": 11705369183,
  "lastblock": 3
}

