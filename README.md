# ETO Travel MCP Server

MCP-compatible HTTP server for searching tours via eto.travel (Tourvisor backend).

Проект реализует MCP-сервер на FastAPI, который предоставляет инструмент
`search_tours` и возвращает реальные туры с ценами, датами, ночами, питанием
и информацией об отелях.

---

## 🚀 Возможности

- MCP-style API (HTTP)
- Реальная интеграция с backend eto.travel (Tourvisor)
- Получение живых туров:
  - цена
  - дата вылёта
  - количество ночей
  - тип питания
  - город
  - отель (название, звёзды, рейтинг, фото)
- Swagger UI (`/docs`)
- Демонстрационный агент-клиент (LLM-like), использующий MCP API

---

## 🧠 Архитектура

Поиск туров в Tourvisor состоит из двух этапов:

1. **Start search** — создаёт `requestid` (на стороне frontend eto.travel)
2. **Get results** — polling результатов поиска по `requestid`

В рамках тестового задания реализован второй этап:
сервер принимает `requestid` и получает результаты через endpoint:

https://search3.tourvisor.ru/modresult.php

> `requestid` берётся из браузерного поиска eto.travel (DevTools → Network).

---

## 📡 MCP API

### POST /mcp/get_results

Получить результаты поиска по `requestid`.

Пример запроса:
{
  "requestid": 11705369183,
  "lastblock": 3
}

---

### POST /mcp/search_tours

Обёртка над get_results, предназначенная для использования агентами / LLM.

---

## 🧪 Swagger

После запуска сервера доступен Swagger UI:

http://localhost:8000/docs

---

## 🤖 Agent / LLM demo

В проекте есть client.py — демонстрационный агент, который:

1. Запрашивает туры через MCP API
2. Получает и отображает реальные предложения

Запуск:
python3 client.py

---

## 🛠️ Установка и запуск (локально)

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
uvicorn server:app --reload

---

## 📦 Технологии

- Python 3.9+
- FastAPI
- Uvicorn
- Requests
- Pydantic

---

## ⚠️ Примечание (Demo mode)

Внешний источник данных (eto.travel / Tourvisor) использует временные search requestid,
которые имеют ограниченное время жизни и могут истекать.

Для обеспечения воспроизводимой демонстрации MCP-интерфейса
сервер поддерживает **demo fallback режим**:

- при недоступности или истечении requestid
- сервер возвращает зафиксированный реальный результат поиска
- ответ явно помечается как `mode: demo_fallback` и содержит предупреждение

Это позволяет проверяющему всегда получить корректный ответ от MCP-сервера
и оценить архитектуру, формат данных и поведение API
без зависимости от внешнего сервиса.

В production-версии предполагается реализация полного цикла поиска:
`start_search → get_results`.

---

## 📌 Статус

Тестовое задание выполнено  
Реальная интеграция с eto.travel  
MCP-сервер доступен через HTTP

